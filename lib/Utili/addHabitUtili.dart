import 'package:flutter/cupertino.dart';
import 'package:flutter/widgets.dart';

/*class AddHabitUtil {
  Widget _widget;
  VoidCallback _callback;


  Widget get widget => _widget;

  set widget(Widget value) {
    _widget = value;
  }


  AddHabitUtil(this._widget, this._callback);

  static void showDateBottomSheet (BuildContext context,
          {required Widget child, required VoidCallBack onClicked }) =>
      showCupertinoModalPopup(
          context: context,
          builder: (context) => CupertinoActionSheet(
                actions: _widget,
                cancelButton: CupertinoActionSheetAction( child: Text("Continue"), onPressed: onClicked,),
              ));

  VoidCallback get callback => _callback;

  set callback(VoidCallback value) {
    _callback = value;
  }
}*/
